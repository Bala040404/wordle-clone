import { SingleCharInputDirective } from './single-char-input.directive';

describe('SingleCharInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleCharInputDirective();
    expect(directive).toBeTruthy();
  });
});
